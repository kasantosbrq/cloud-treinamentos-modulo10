version: 0.2

phases:
  pre_build:
    commands:
      - echo install dependencias
      - npm install


  build:
    commands:
      - echo Build started on `date`
      - npm run build

  post_build:
    commands:
      - echo Build Finish on `date`

artifacts:
  files:
    - build/**/*